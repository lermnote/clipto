/**app.wxss**/

/* 重置 button 默认样式 */
button {
  background: initial;
  padding: 0;
  margin: 0;
  line-height: inherit;
}

button:focus {
  outline: 0;
}

button::after {
  border: none;
}

/* 全局 page */
page {
  background: #f0f2f5;
  display: flex;
  flex-direction: column;
}
